def sum_of_digits(n):
    total = 0
    n = abs(n)
    while n > 0:
        digit = n % 10
        total += digit
        n //= 10
    return total

num = int(input("Enter a number: "))
print(f"Sum of digits: {sum_of_digits(num)}")
